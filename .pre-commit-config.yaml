repos:
  - repo: local
    hooks:
      - id: ruff-format
        name: ruff format
        entry: ruff format
        args: [--force-exclude]
        language: system
        types_or: [python, pyi]
